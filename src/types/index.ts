export type Option = {
  label: string;
  value: string;
};

export type Brand = {
  viewBox: string;
  d: string;
  brandUrl: string;
};

export type Blog = {
  title: string;
  titleUrl: string;
  abstract: string;
  blogUrl: string;
  postedAt: string;
  tagName: string;
  tagUrl: string;
  blogAuthor: string;
  authorImage: string;
};
